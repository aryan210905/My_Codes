def sum(a,b):
    sum = a+b
    return sum
a = int(input("Enter first number: "))
b = int(input("Enter second number: "))
print(sum(a,b))

def product(a,b):
    pass    # pass is used when we just want to declare function but not want to provide
            # the function definition for now